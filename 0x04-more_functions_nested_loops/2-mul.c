#include "holberton.h"

/**
 * mul - multiplies two integer .
 *
	* @a: first number.
	* @b: second number.
 * Return: the multiplication of two numbers .
 */
int mul(int a, int b)
{
return (a * b);
}
